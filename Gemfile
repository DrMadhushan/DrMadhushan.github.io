source "https://rubygems.org"

# Pin to GitHub Pages to match the Pages build environment.
gem "github-pages", group: :jekyll_plugins

# Optional: uncomment to run `jekyll doctor` and other utilities locally.
# gem "jekyll", "~> 3.9"
